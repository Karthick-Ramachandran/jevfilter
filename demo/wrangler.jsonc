{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "jevfilter",
  "main": "src/worker.ts",
  "compatibility_date": "2026-09-23",
  "compatibility_flags": ["nodejs_compat"],
  "assets": {
    "directory": "./public",
    "binding": "ASSETS",
    "run_worker_first": ["/api/*"],
    "not_found_handling": "404-page"
  },
  // ADR-0010: the shared Jev key is limited to 10/min per IP and 60/min globally (spend cap, ~$8-11/day worst case).
  // The public site is the Pages project in ./pages, which reaches this Worker through a service binding.
  "ratelimits": [
    { "name": "IP_LIMITER", "namespace_id": "4101", "simple": { "limit": 10, "period": 60 } },
    { "name": "GLOBAL_LIMITER", "namespace_id": "4102", "simple": { "limit": 60, "period": 60 } }
  ],
  // ADR-0010: shared answer cache. Only model answers under hashed keys; no search text or records.
  "kv_namespaces": [{ "binding": "ANSWER_CACHE", "id": "5d9b0d2c3fea46ada53d955ef2eed038" }],
  "vars": {
    // Kill switch: set to "1" and redeploy to stop all model calls.
    "DEMO_DISABLED": "0"
  },
  "observability": { "enabled": true }
}
